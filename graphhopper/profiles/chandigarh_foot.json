{
  "priority": [
    { "if": "true", "multiply_by": 1.0 },
    { "if": "footway == SIDEWALK", "multiply_by": 1.3 },
    { "if": "road_class == RESIDENTIAL", "multiply_by": 1.1 },
    { "if": "road_class == SERVICE", "multiply_by": 1.0 },
    { "if": "road_class == MOTORWAY || road_class == TRUNK || road_class == PRIMARY", "multiply_by": 0.1 }
  ],
  "speed": [
    { "if": "true", "limit_to": 10 },
    { "if": "footway == SIDEWALK", "limit_to": 5 },
    { "if": "road_class == RESIDENTIAL", "limit_to": 4 },
    { "if": "road_class == SERVICE", "limit_to": 5 }
  ]
}