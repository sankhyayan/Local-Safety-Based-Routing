{
  "priority": [
    { "if": "true", "multiply_by": 1.0 },
    { "if": "road_class == CYCLEWAY", "multiply_by": 1.5 },
    { "if": "road_class == MOTORWAY || road_class == TRUNK", "multiply_by": 0.1 },
    { "if": "road_class == RESIDENTIAL", "multiply_by": 1.2 }
  ],
  "speed": [
    { "if": "true", "limit_to": 25 },
    { "if": "road_class == MOTORWAY || road_class == TRUNK", "limit_to": 10 },
    { "if": "road_class == PRIMARY", "limit_to": 25 },
    { "if": "road_class == SECONDARY", "limit_to": 20 },
    { "if": "road_class == TERTIARY", "limit_to": 18 },
    { "if": "road_class == RESIDENTIAL", "limit_to": 15 },
    { "if": "road_class == CYCLEWAY", "limit_to": 25 }
  ]
}